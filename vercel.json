{
  "version": 2,
  "name": "notes-app",
  "buildCommand": "cd frontend && npm install && npm run build -- --configuration=production",
  "outputDirectory": "frontend/dist/notes-app",
  "framework": "angular",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}